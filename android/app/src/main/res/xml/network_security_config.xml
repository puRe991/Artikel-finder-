<?xml version="1.0" encoding="utf-8"?>
<!--
  Die Entwicklungs-API läuft über HTTP im lokalen Netz. Klartext ist deshalb nur für
  private Adressen erlaubt — alles andere muss auch im Debug-Build über HTTPS laufen.
  Vor einer Veröffentlichung (Phase 3) gehört hier ein echter HTTPS-Endpunkt hin und
  dieser Block raus.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Typische Heimnetz-Bereiche für den Test auf einem echten Gerät. -->
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">192.168.1.0</domain>
        <domain includeSubdomains="true">192.168.2.0</domain>
        <domain includeSubdomains="true">192.168.178.0</domain>
    </domain-config>
</network-security-config>
