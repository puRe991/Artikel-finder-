# Room und Hilt bringen ihre eigenen Regeln mit. Hier bleibt nur, was die App selbst
# per Reflection braucht — derzeit nichts, weil kein JSON mehr serialisiert wird.
-keepattributes *Annotation*, InnerClasses
